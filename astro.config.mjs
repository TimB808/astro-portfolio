// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  output: 'static', // Explicitly set output to static
  base: '/', // Set the base URL (if needed)
});
